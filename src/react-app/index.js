import React, { Fragment } from 'react';
import ReactDOM from 'react-dom';

import AppMain from './components/AppMain/AppMain.jsx';

const targetEl = document.getElementById('app-root');

ReactDOM.render(<AppMain />, targetEl);
