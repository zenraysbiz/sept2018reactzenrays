const userName = 'John';
const city = 'Bangalore';
const age = 18;

// const welcomeMessage = 'Hello, ' + userName + ' staying in ' + city + ' and age is ' + 18;
const welcomeMessage = `Hello ${userName} staying in
 ${city} and age is ${age}`;
console.log('welcomeMessage: ', welcomeMessage);
